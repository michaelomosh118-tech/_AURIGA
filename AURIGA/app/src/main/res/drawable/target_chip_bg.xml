<?xml version="1.0" encoding="utf-8"?>
<!--
    Unselected chip: dark glass card with a subtle cyan border so the
    chip reads as interactive against the deep_space background but
    doesn't compete with the radar / readouts on the HUD.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#0F2A33" />
    <stroke android:width="1dp" android:color="#264E5C" />
    <corners android:radius="10dp" />
</shape>
