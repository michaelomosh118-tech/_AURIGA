{
  "name": "auriga-replit",
  "version": "1.0.0",
  "private": true,
  "description": "Replit dev server + Netlify deploy tooling for the Auriga ecosystem.",
  "scripts": {
    "start": "node server.js",
    "deploy": "node scripts/deploy-netlify.js",
    "deploy:draft": "node scripts/deploy-netlify.js --draft"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.36.2"
  }
}
